//Math.random()

// Generate a decimal nuber between 0 and 0.99
Math.random();

//Save it to a variable
var decimal = Math.random();

//Log the value of decimal to the console
console.log(decimal);

//Log the value of decimal MULTIPLIED BY10
console.log(decimal * 10);
